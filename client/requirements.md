## Packages
framer-motion | For smooth animations of result cards and layout transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
The app requires a font that supports tabular figures for data density.
Integration with the provided API endpoints for keyword search and saving is required.
